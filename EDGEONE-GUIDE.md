# ✏️ EdgeOne 函数编辑器使用指南

## 🔍 快速定位函数编辑器

### **腾讯云 EdgeOne（最可能）**

#### 方式 1：通过腾讯云控制台
```
1. 打开 https://console.cloud.tencent.com/
2. 在顶部搜索框输入 "边缘函数" 或 "EdgeOne"
3. 点击进入 EdgeOne 产品控制台
4. 在左侧菜单找 "函数管理" 或 "Functions"
5. 你会看到函数列表，找 "read-chinese-menu" 或 "/api/gemini"
6. 点击函数名称进入详情页
7. 在标签栏找 "函数代码" 或 "Code"
8. 这就是代码编辑器！
```

#### 方式 2：直接访问（如果你有项目 ID）
```
https://console.cloud.tencent.com/edgeone/functions
```

---

### **其他 EdgeOne 平台**

如果上面的步骤不适用，请告诉我：
- EdgeOne 提供商是谁（腾讯、阿里、华为 等）
- 控制台的 URL 是什么
- 你能看到函数列表吗？

---

## 📝 在函数编辑器中进行的操作

### **第 1 步：选择你的函数**
找到 `/api/gemini` 函数，点击进入

### **第 2 步：查看当前代码**
你应该能看到一个代码编辑器，显示类似的内容：
```javascript
export async function onRequestPost(context) {
  // ... 你的函数代码
}
```

### **第 3 步：替换代码**

**现在有两个选择：**

#### 选项 A：使用 Mock 版本（快速测试）
如果 545 错误持续，先用简化的 mock 版本测试基本流程是否工作：

1. 打开 [edge-functions/api/gemini-test.js](../edge-functions/api/gemini-test.js)
2. 复制全部代码
3. 粘贴到 EdgeOne 函数编辑器，**完全替换现有代码**
4. 点击 **部署** 或 **保存并发布**
5. 回到网站，尝试上传菜单图片
6. 如果能看到 2 个菜肴（宫保鸡丁、麻婆豆腐），说明基本流程工作正常

#### 选项 B：修复完整的 Gemini 版本

如果 mock 版本成功，说明问题在 Gemini API 集成上。可能是：
- 网络连接问题
- 函数超时
- 环境变量配置问题

此时请：
1. 打开 [edge-functions/api/gemini.js](../edge-functions/api/gemini.js)
2. 复制全部代码到 EdgeOne 编辑器
3. 确认环境变量已设置（见下面的步骤）

---

## ⚙️ 环境变量配置

在 EdgeOne 函数编辑器中：

1. **找到 "环境变量" 或 "Environment Variables" 选项**
   - 可能在右侧面板
   - 可能在 "配置" 标签中
   - 可能需要点击函数名旁的 "⚙️ 设置"

2. **添加环境变量**
```
变量名: GEMINI_API_KEY
变量值: 你的_实际_API_Key
```

3. **保存**

4. **重新部署函数**

---

## 🐛 故障排除

### **看不到代码编辑器？**

如果 EdgeOne 控制台没有代码编辑器，可能是：

1. **函数使用了其他部署方式**
   - 检查是否通过 CLI 上传（需要在本地修改源码再重新部署）
   - 检查是否使用了 Git 集成（需要 push 代码到仓库）

2. **需要启用代码编辑功能**
   - 检查函数设置中是否有 "启用在线编辑" 选项

3. **没有权限**
   - 确认你的账户有足够的权限编辑函数

### **代码编辑器是只读的？**

- 检查函数是否处于 "运行中" 状态
- 有些平台不允许编辑运行中的函数，需要停止后再编辑
- 或者需要先将函数设置为草稿状态

---

## ✅ 验证编辑成功

编辑完代码后，你应该看到：

1. **代码被接受**
   - 编辑器没有语法错误提示（通常是红色波浪线）

2. **部署成功**
   - 有 "部署成功" 或 "发布成功" 的提示
   - 或在日志中看到成功信息

3. **可以在日志中看到新的日志消息**
   - 如果用 mock 版本，应该看到 `[Test API] Request received`
   - 如果是完整版本，应该看到 `[Env Debug]` 开头的日志

---

## 🔄 如果还是找不到？

请提供以下信息，我会更具体地帮助你：

1. **你打开的 EdgeOne 控制台 URL 是什么？**
   - 例如：`console.cloud.tencent.com` / `aliyun.com` / 等

2. **左侧菜单中有哪些选项？**
   - 例如：Functions / Workers / 边缘函数 / 等

3. **你的函数是如何部署的？**
   - 通过控制台 UI
   - 通过 CLI（wrangler / aliyun-cli / 等）
   - 通过 Git

4. **能否截图或描述控制台界面？**

有了这些信息，我可以给你更准确的导航步骤！
